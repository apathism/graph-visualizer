/*
 * Это тестовая программа для визуализатора. Она порождает по исходной карте
 * точно такую же.
 */

#include <cstdio>

int main() {
	int ch;
	while ((ch = getchar()) != EOF) {
		putchar(ch);
	}
	return 0;
}
